export const APP_NAME = "Reqlight";
export const SAVE_DEBOUNCE_MS = 500;
export const DEFAULT_REQUEST_TIMEOUT = 30;
export const TOAST_DISMISS_MS = 2000;
export const MAX_HISTORY_ENTRIES = 100;

export const SPACING = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
} as const;

export const DIMENSIONS = {
  sidebarMinWidth: 200,
  sidebarDefaultWidth: 220,
  sidebarMaxWidth: 320,
  editorMinWidth: 380,
  responseMinWidth: 300,
  windowMinWidth: 1000,
  windowMinHeight: 600,
  methodPickerWidth: 80,
  methodBadgeWidth: 38,
  kvRowHeight: 32,
  kvHeaderHeight: 28,
  headerNameColumnWidth: 200,
} as const;
